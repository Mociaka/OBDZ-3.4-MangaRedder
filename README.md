# 📚 MangaReader - Flask Manga Reader Application

Монолітний Flask додаток для читання манги з PostgreSQL базою даних.

## ✨ Особливості

- 🏠 **Головна сторінка** - перегляд усіх доступних манг
- 📖 **Сторінка читання** - постатейний перегляд манги з навігацією між розділами
- ⚡ **Адмін-панель** - додавання манги, розділів та сторінок
- 🎨 **Сучасний дизайн** - темна тема з градієнтами та анімаціями
- 🐳 **Docker Compose** - легке розгортання з PostgreSQL

## 🛠️ Технології

- **Backend**: Flask 3.0
- **Database**: PostgreSQL 15
- **ORM**: SQLAlchemy
- **Containerization**: Docker & Docker Compose

## 📋 Передумови

- Docker Desktop для Windows
- Git (опціонально)

## 🚀 Швидкий старт

### 1. Запуск через Docker Compose (рекомендовано)

```bash
# Перейдіть до каталогу проекту
cd mangaredder

# Запустіть Docker Compose
docker-compose up -d

# Переконайтеся, що контейнери запущені
docker-compose ps
```

Додаток буде доступний за адресою: http://localhost:5000

### 2. Запуск локально (без Docker)

```bash
# Встановіть залежності
pip install -r requirements.txt

# Запустіть PostgreSQL окремо (або використовуйте тільки БД з docker-compose)
docker-compose up -d db

# Запустіть Flask додаток
python run.py
```

## 📖 Використання

### Додавання манги

1. Перейдіть до адмін-панелі: http://localhost:5000/admin
2. Заповніть форму "Додати мангу":
   - Назва (обов'язково)
   - Автор
   - URL обкладинки
   - Опис
3. Натисніть "Додати мангу"

### Додавання розділів

1. В адмін-панелі знайдіть форму "Додати розділ"
2. Оберіть мангу зі списку
3. Вкажіть номер розділу (наприклад, 1 або 1.5)
4. Додайте назву розділу (опціонально)
5. Натисніть "Додати розділ"

### Додавання сторінок

1. В адмін-панелі знайдіть форму "Додати сторінки"
2. Оберіть мангу
3. Оберіть розділ
4. Вставте URL зображень (по одному на рядок):
   ```
   https://example.com/page1.jpg
   https://example.com/page2.jpg
   https://example.com/page3.jpg
   ```
5. Натисніть "Додати сторінки"

### Читання манги

1. На головній сторінці оберіть мангу
2. Оберіть розділ зі списку
3. Гортайте сторінки вниз
4. Використовуйте кнопки навігації для переходу між розділами

## 🗄️ Структура бази даних

### Таблиця `manga`
- `id` - первинний ключ
- `title` - назва манги
- `description` - опис
- `cover_url` - URL обкладинки
- `author` - автор
- `created_at` - дата створення

### Таблиця `chapters`
- `id` - первинний ключ
- `manga_id` - зовнішній ключ до manga
- `chapter_number` - номер розділу
- `title` - назва розділу
- `created_at` - дата створення

### Таблиця `pages`
- `id` - первинний ключ
- `chapter_id` - зовнішній ключ до chapters
- `page_number` - номер сторінки
- `image_url` - URL зображення

## 🛑 Зупинка додатку

```bash
# Зупинити контейнери
docker-compose down

# Зупинити та видалити volumes (увага: видалить дані БД!)
docker-compose down -v
```

## 🔧 Налаштування

Змінні середовища можна налаштувати в `docker-compose.yml`:

- `POSTGRES_USER` - ім'я користувача БД (за замовчуванням: manga_user)
- `POSTGRES_PASSWORD` - пароль БД (за замовчуванням: manga_password)
- `POSTGRES_DB` - назва БД (за замовчуванням: manga_db)

## 📝 Примітки

- Зображення зберігаються як URL, не завантажуються на сервер
- Переконайтеся, що URL зображень доступні публічно
- Використовуйте якісні зображення для кращого досвіду

## 🐛 Налагодження

### Перегляд логів

```bash
# Логи всіх сервісів
docker-compose logs

# Логи тільки Flask
docker-compose logs web

# Логи тільки PostgreSQL
docker-compose logs db

# Слідкувати за логами в реальному часі
docker-compose logs -f
```

### Підключення до бази даних

```bash
# Підключитися до PostgreSQL
docker exec -it manga_postgres psql -U manga_user -d manga_db
```

## 📜 Ліцензія

Вільне використання для особистих та навчальних цілей.
